// MANUAL OVERRIDE - Fixed version
android {
  compileOptions {
      sourceCompatibility JavaVersion.VERSION_21
      targetCompatibility JavaVersion.VERSION_21
  }
}

dependencies {
    implementation "com.capacitorjs:core:6.0.0"
}

if (hasProperty('postBuildExtras')) {
  postBuildExtras()
}
